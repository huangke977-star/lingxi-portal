# HLOVET 发现与个人主页

- 对应阶段：P4 发现与个人主页
- 文档日期：2026-08-10
- 当前状态：P4 主功能已部署；本次专题与合集交互增强已完成本地开发，等待用户确认后提交部署
- 英文版本：`docs/discovery-and-profiles.en.md`

## 一、功能入口

| 功能 | 入口 | 权限 |
| --- | --- | --- |
| 订阅动态 | 发现 -> 订阅 | 登录用户 |
| 我的合集与可见合集 | 发现 -> 合集 | 登录用户，浏览结果按当前账号权限过滤 |
| 专题列表 | 发现 -> 专题 | 游客和登录用户，内容按权限过滤 |
| 专题管理 | 专题 -> 管理专题，或 `/admin/topics` | 管理员、超级管理员 |
| 主页展示设置 | 个人中心 -> 主页展示 | 当前账号 |
| 用户公开主页 | 用户头像、昵称或 `/users/:username` | 游客和登录用户 |

## 二、订阅动态

订阅动态从现有作者订阅关系生成，不复制文章正文，也不增加新的推荐服务。

- `最新`：按发布时间和文章 ID 倒序。
- `未读`：未读文章优先，同一组内仍按发布时间倒序。
- `热度`：依次按评论、收藏、点赞、阅读和发布时间排序。
- 单条已读记录存入 `subscription_feed_reads`，因此换浏览器或设备后仍保持一致。
- `全部已读` 使用批量写入和唯一键去重，不会重复生成记录。
- 作者级推送开关只控制“新文章通知”，关闭后仍保留订阅关系和订阅动态。
- 文章发布通知只发送给启用了 `notify_new_articles` 的订阅者。

## 三、文章合集

每个用户只能管理自己的合集，合集删除不会删除文章。

- 支持创建、修改、删除和合集内文章排序。
- “我的合集”使用可搜索多选面板即时加入或移出文章，已选文章通过拖动手柄排序，不再使用加入按钮和上下箭头。
- “浏览合集”可以按合集名称、说明、作者昵称或用户名模糊搜索当前账号有权查看的全部合集，并向下滚动追加结果。
- 只能加入当前账号自己的有效文章。
- 可见范围包括：公开、登录可见、仅自己。
- 排序接口要求提交当前合集的完整文章 ID 集合；缺少、重复或混入其他文章都会拒绝。
- 公开合集可以通过 `/collections/:id` 分享，登录可见和私有合集会在 API 层继续校验访问者。

## 四、管理员专题

专题用于跨作者组织文章，由管理员和超级管理员共同维护。

- 支持标题、路径标识、封面图片网址、本地封面上传、说明、状态和排序。
- 本地封面支持 JPEG、PNG、WebP 和 AVIF，单张不超过 10 MB；替换封面或删除专题时会同时物理删除原文件。
- 本地专题封面归入现有文章媒体目录、存储巡检和异地媒体备份目录，不会被误判为孤立文件。
- 可见范围包括：公开、登录可见、指定角色。
- 指定角色专题必须至少选择一个有效角色。
- 停用专题不会出现在普通专题列表中。
- 专题内文章与合集使用相同的可搜索多选和拖动排序交互，成员与顺序即时保存，标题等基础信息由页头保存按钮提交。
- 专题删除不会删除文章。

普通文章卡片和阅读页只返回当前访问者有权打开的合集与专题标签：游客只看公开归属，登录用户可看登录可见归属，指定角色专题按角色过滤，私有合集只对所有者和超级管理员显示。

## 五、个人主页

用户可以在个人中心控制以下公开字段：

- 个人介绍。
- 加入时间。
- 内容与访问统计。
- 已订阅数量。
- 代表内容。

代表内容包括一篇已发布文章和一个自己的合集。关闭“代表内容”后，公开主页不返回也不显示相应内容。

公开主页可以展示：

- 代表文章与代表合集。
- 当前访问者可见的文章合集。
- 公开文章、获赞、阅读、订阅者、已订阅和主页访问统计。

当前用户查看自己的主页时仍可看到自己的完整信息，方便确认设置；其他访问者严格按隐私开关接收字段。

## 六、访问统计与隐私

主页访问记录按“访问者 + 日期”去重：

- 登录访问者使用用户 ID 生成 SHA-256 哈希。
- 匿名访问者使用 IP 与 User-Agent 生成 SHA-256 哈希。
- 数据库只保存 64 位哈希和日期，不保存原始 IP 或 User-Agent。
- 用户关闭统计公开后，API 不返回访问量，也不会额外查询其他公开统计。
- 用户访问自己的主页不计入访问量。

该统计用于提供轻量级访问趋势，不用于精确设备识别或跨站追踪。

## 七、主要 API

| 路径 | 作用 |
| --- | --- |
| `GET /discovery/feed` | 订阅动态、未读数和排序 |
| `POST /discovery/feed/:articleId/read` | 单条已读 |
| `POST /discovery/feed/read-all` | 全部已读 |
| `GET/PATCH /discovery/subscriptions/...` | 作者推送设置 |
| `GET /discovery/collections`、`GET /discovery/collections/visible` | 分页搜索公开或当前用户可见合集 |
| `GET/POST/PATCH/DELETE /discovery/collections/...` | 自有合集管理与排序 |
| `GET /discovery/topics...` | 公开或当前用户可见专题 |
| `GET/POST/PATCH/DELETE /discovery/admin/topics...` | 管理员专题管理 |
| `POST /discovery/admin/topics/:id/cover` | 上传并立即替换专题本地封面 |
| `GET /discovery/topics/covers/:storedName` | 读取受管理的专题封面文件 |
| `GET/PATCH /discovery/profile/settings` | 主页隐私和代表内容设置 |
| `GET /discovery/profiles/.../:username` | 主页代表内容、合集和访问统计 |

## 八、数据迁移

迁移：`20260810100000_add_discovery_profiles`、`20260810120000_add_topic_cover_metadata`

新增内容包括：

- 订阅作者推送开关与订阅动态已读记录。
- 文章合集、合集文章关系和排序。
- 管理员专题、专题角色、专题文章关系和排序。
- 用户主页设置、代表文章、代表合集。
- 隐私友好的主页访问记录。
- 专题本地封面的原始文件名、磁盘文件名、MIME 类型和大小元数据。

迁移均为增量变更，没有删除或重命名现有字段。首个 P4 迁移已部署；新的专题封面元数据迁移将在本次修改确认后随部署执行。

## 九、验证结果

- Prisma 格式化、校验和客户端生成通过。
- API 30 个测试套件共 198 条测试通过。
- API Lint、API 构建和 Web 构建通过。
- Web Lint 无错误，保留 10 条与 P4 无关的既有警告。
- Playwright 已检查移动端专题整卡点击、合集浏览与选择面板、专题管理和复选框状态；既有 P4 验收已覆盖订阅流、主页展示和公开主页。
- `1440px` 桌面宽度与 `390px` 移动宽度均无横向溢出。

生产部署后仍需验证真实账号之间的订阅发布、跨设备已读、角色专题可见性和隐私字段隐藏。
