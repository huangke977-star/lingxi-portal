# HLOVET 产品与技术路线图

- 文档状态：生效中
- 建立日期：2026-08-04
- 最近更新：2026-08-24
- 当前阶段：第十阶段已完成
- 对应英文文档：`docs/six-phase-roadmap.en.md`

## 一、文档用途

本文档是 HLOVET 后续开发的长期执行依据。后续开始新任务或新对话时，应先读取本文档，从第一个未完成的任务编号继续，并在开发、测试和部署后同步更新状态与完成记录。

状态定义：

- `未开始`：尚未进入开发。
- `进行中`：已经开始设计或实现。
- `已完成`：代码、测试、文档和生产验证均已完成。
- `阻塞`：缺少外部配置、用户决策或第三方服务。

## 二、总体原则

1. 所有阶段属于同一份长期计划，但必须分阶段开发和部署。
2. 每个阶段都必须可以独立上线和回滚，不积累到最后一次性发布。
3. 数据库迁移优先使用增量、兼容性变更，避免直接破坏现有数据。
4. 未完成或未验收的功能通过权限或功能开关隐藏。
5. 继续复用 MySQL、Redis、Socket.IO、现有消息系统和现有 OSS/R2 配置。
6. 不引入 Elasticsearch、Prometheus、Grafana、ClickHouse 等高资源常驻服务。
7. 后台任务限制并发和磁盘 I/O，避免再次造成服务器卡顿。
8. 所有新增文档同时提供中文和英文版本。
9. 每个阶段完成后更新本文档、提交编号、部署时间和生产验证结果。

## 三、阶段总览

| 阶段 | 名称 | 主要范围 | 当前状态 |
| --- | --- | --- | --- |
| 第一阶段 | 可靠性基础 | 媒体备份、缺失文件修复、轻量监控 | 已完成（远端恢复演练暂免） |
| 第二阶段 | 账号安全 | 密码找回、邮件验证、Turnstile、登录风险提醒 | 已完成 |
| 第三阶段 | 内容能力 | 自动保存、版本历史、发布预览、统一搜索 | 已完成 |
| 第四阶段 | 发现与个人主页 | 订阅动态、专题合集、个人主页增强 | 已完成 |
| 第五阶段 | 社交能力 | 群聊、群文件、群管理、临时会话 | 已完成 |
| 第六阶段 | 运营后台 | 运营统计、公告、定时发布和阅读统计 | 已完成 |
| 第七阶段 | 备份恢复闭环 | 数据库与媒体配对快照、完整性验证、恢复预检 | 已完成 |
| 第八阶段 | 门户入口升级 | 首页、工作台和工具中心重构 | 已完成 |
| 第九阶段 | 隐私与发现扩展 | 社交权限、搜索范围和发现入口 | 已完成 |
| 第十阶段 | 内容治理自动化 | 统一举报、反垃圾和审核规则 | 已完成 |

## 四、开始前需要的外部配置

以下配置只在对应阶段开始前提供，不阻塞其他阶段：

| 配置 | 使用阶段 | 说明 |
| --- | --- | --- |
| Cloudflare R2 或阿里云 OSS 凭据 | 第一阶段 | 支持两种提供商，实际至少启用一种 |
| SMTP 发信配置 | 第二阶段 | 用于验证码、密码找回和登录提醒 |
| Cloudflare Turnstile Site Key 与 Secret Key | 第二阶段 | 用于注册和高风险操作的人机验证 |
| 群聊人数上限 | 第五阶段 | 默认建议 100 人，可在开始前调整 |

## 五、第一阶段：可靠性基础

目标：让数据库记录和实际文件都具备可核查、可备份、可恢复的完整生命周期，并提供不依赖大型监控组件的基础运行观测能力。

### 开发任务

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P1-01 | 建立媒体备份任务、备份文件和备份清单数据模型 | 已完成 |
| P1-02 | 覆盖背景、站点资源、APK、头像、文章图片和聊天附件六类文件 | 已完成 |
| P1-03 | 根据文件哈希、大小和备份清单执行增量备份 | 已完成 |
| P1-04 | 复用现有 OSS/R2 加密配置，支持启用、停用和连接测试 | 已完成 |
| P1-05 | 排除 `.tmp`、`.trash` 和未完成上传，默认并发限制为 1 | 已完成 |
| P1-06 | 支持定时备份、保留策略、失败重试和任务日志 | 已完成 |
| P1-07 | 支持单文件恢复，先恢复到临时目录，校验后原子替换 | 已完成 |
| P1-08 | 缺失文件增加“远端恢复、重新上传、确认无法恢复”处理流程 | 已完成 |
| P1-09 | 增加慢接口、最近 API 错误、内存和磁盘趋势的轻量统计 | 已完成 |
| P1-10 | 在系统概述展示备份覆盖率、最近成功时间和异常数量 | 已完成 |
| P1-11 | 备份失败、磁盘超限和缺失文件变化时通知超级管理员 | 已完成 |
| P1-12 | 完成中英文文档、自动化测试、部署与生产恢复演练 | 已完成（远端演练暂免） |

### 当前验收结果

- P1-01 至 P1-11 的代码、迁移、页面、自动化测试和生产部署已经完成。
- API 26 套共 159 项测试、前后端 Lint/构建、Prisma 生成/校验和生产 Compose 校验通过。
- GitHub Actions `30986260663` 与热修复构建 `30987181919` 成功；生产只重建 API/Web 或 API，没有无关重启 MySQL、Redis、Caddy、TURN。
- 生产存储扫描 `#5` 完成：20 个健康文件、3 个历史缺失文章文件、0 个孤立文件；桌面与 390px iPhone 宽度没有横向溢出。
- 当前未配置 OSS/R2 和远端凭据，真实远端上传与生产恢复演练尚不能执行。用户于 2026-08-10 确认该项在具备远端服务前暂免验证，不再阻塞后续阶段；功能、配置入口和本地流程保留，后续具备服务时再补做真实演练。

### 验收标准

1. 六类持久化文件都能生成备份清单。
2. 未变化文件不会在每次任务中重复上传。
3. 备份执行期间网站登录、文章和聊天接口保持可用。
4. 能从远端恢复一个测试文件，并通过哈希和大小校验。
5. 缺失文件可以重新上传或从备份恢复，处理状态会被记录。
6. 系统概述可以看到最近备份、未备份数量和任务失败状态。

### 暂不包含

- 整机镜像备份。
- Docker 卷快照。
- 任意宿主机目录备份。
- 引入新的常驻备份服务。

## 六、第二阶段：账号安全

目标：在开放注册的前提下补齐账号找回、机器人防护和登录风险感知。

### 开发任务

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P2-01 | 增加 SMTP 管理配置、连接测试和凭据加密保存 | 已完成 |
| P2-02 | 注册邮箱验证码与验证码频率限制 | 已完成 |
| P2-03 | 忘记密码、重置令牌和全部旧会话失效 | 已完成 |
| P2-04 | 注册、登录失败过多和密码找回接入 Turnstile | 已完成 |
| P2-05 | 识别新设备、陌生 IP 和异常登录频率 | 已完成 |
| P2-06 | 通过站内消息和邮件发送登录风险提醒 | 已完成 |
| P2-07 | 在个人中心增加账号安全日志和最近安全事件 | 已完成 |
| P2-08 | 增加登录提醒、邮件提醒和新设备提醒开关 | 已完成 |
| P2-09 | 管理后台提供验证码、邮件任务和风险事件查询 | 已完成 |
| P2-10 | 完成限流、令牌失效、安全测试和中英文文档 | 已完成 |
| P2-11 | 增加非信任设备邮箱验证、信任设备管理和活动会话逐条退出 | 已完成 |

### 当前验收结果

- P2-01 至 P2-10 的代码、增量迁移、桌面与移动端页面、权限控制、自动化测试和中英文文档已经完成。
- API 27 套共 173 项测试全部通过；Prisma 生成/校验、前后端 Lint 与生产构建、生产 Compose 配置校验均已通过。
- 新功能默认关闭，历史账号迁移后保持邮箱已验证状态；重置密码会撤销全部 Refresh Token，并通过账号安全版本立即作废旧 Access Token。
- 提交 `a22a04f` 已推送，GitHub Actions `31064603456` 的 API/Web 镜像构建成功；生产于 2026-08-06 应用第 28 个迁移并只重建 API/Web，MySQL、Redis、Caddy、TURN 均未重启。
- 首页、登录、注册、找回密码、个人中心、安全管理页和健康接口均返回 `200`；公开安全策略、未登录 `401`、默认关闭状态、服务器凭据加密环境与 390px 移动端无横向溢出均已验证，API/Web 启动日志无错误。
- SMTP 注册验证、密码找回和 Turnstile 已在生产手动验证；P2-11 的非信任设备邮箱验证仍默认关闭，需保留一个已登录超级管理员会话后再受控开启并验收真实设备邮件流程。
- P2-11 提交 `8e97c4c` 与生产修复 `c139889` 已推送，Actions `31152045165`、`31152920263` 均成功。生产于 2026-08-07 应用第 29 个迁移并只重建 API/Web；验收发现并回退了 Socket.IO 会话校验崩溃，修复后 28 个 API 测试套件共 179 项通过，API 连续健康检查为 `200`、重启计数为 0、日志无错误，14 个 Redis 登录会话保留，MySQL、Redis、Caddy、TURN 未重启。非信任设备邮箱验证、信任设备取消和活动会话逐条退出随后完成真实设备验收，第二阶段结束。

### 验收标准

1. 用户能够通过已验证邮箱重置密码。
2. 重置密码后旧 Refresh Token 全部失效。
3. 机器人注册和高频验证码请求受到限制。
4. 新设备登录会生成可查询的安全事件和提醒。
5. 邮件服务不可用时不会阻塞普通登录，并能在后台看到失败原因。

### 暂不包含

- 强制所有用户绑定邮箱。
- 短信验证码。
- 第三方账号登录。

## 七、第三阶段：内容能力

目标：降低文章编辑丢失风险，并建立跨文章、用户、导航和工具的统一搜索体验。

### 开发任务

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P3-01 | 文章编辑器增加延迟自动保存和保存状态提示 | 已完成 |
| P3-02 | 自动保存失败时保留本地草稿并支持重新提交 | 已完成 |
| P3-03 | 建立文章版本快照、版本列表和版本差异元数据 | 已完成 |
| P3-04 | 恢复历史版本时生成新版本，不覆盖历史记录 | 已完成 |
| P3-05 | 增加发布前预览，预览结果与正式阅读页保持一致 | 已完成 |
| P3-06 | 建立统一搜索索引和标准化搜索字段 | 已完成 |
| P3-07 | 搜索文章、用户、导航和工具，并按类型分组 | 已完成 |
| P3-08 | 支持昵称、用户名、标题、标签、分类和拼音字段匹配 | 已完成 |
| P3-09 | 增加搜索历史、热门搜索和历史清理 | 已完成 |
| P3-10 | 增加搜索排序、权限过滤、分页和性能测试 | 已完成 |
| P3-11 | 完成中英文文档、移动端适配和生产验证 | 已完成 |

### 当前验收结果

- 自动保存、本地恢复、不可变版本快照、版本恢复、发布预览和统一搜索均已完成，并提供中英文说明 `docs/content-reliability-and-search.zh-CN.md` 与 `docs/content-reliability-and-search.en.md`。
- 统一搜索复用现有四类分组与权限规则，支持标准化字段、完整拼音/首字母、历史、热词、排序、分页和权限过滤。
- 增量迁移 `20260807173000_add_content_reliability_and_search` 已在生产应用，生产共 30 个 Prisma 迁移；83 条搜索索引已回填，用户、文章和入口缺失索引均为 0。
- 29 个 API 测试套件共 186 项测试、API/Web 构建与 Lint、Prisma 校验和 `git diff --check` 均通过；Web 仅保留 10 条与本阶段无关的既有警告。
- 提交 `47c7ae3` 已推送，GitHub Actions `31165692942` 成功。生产于 2026-08-07 仅重建 API/Web，MySQL、Redis、Caddy、TURN 未重启；首页、搜索页、健康接口等外部检查均返回 `200`，桌面与 390px 移动端无横向溢出，浏览器控制台无错误。
- 自动保存、草稿恢复、历史版本、发布预览、拼音搜索和权限过滤已于 2026-08-10 完成生产人工验收，第三阶段结束。

### 验收标准

1. 文章编辑过程中无需手动点击即可保存草稿。
2. 页面关闭或网络中断后可以恢复最近内容。
3. 用户可以查看和恢复历史版本。
4. 搜索结果不会泄露无权访问的文章、服务器入口或私有数据。
5. 搜索结果支持分页，并在移动端正常显示。

### 暂不包含

- Elasticsearch 或独立搜索集群。
- 多人同时协作编辑同一篇文章。
- Office 文档在线编辑。

## 八、第四阶段：发现与个人主页

目标：让订阅关系真正形成内容流，并通过专题合集和个人主页加强内容组织。

### 开发任务

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P4-01 | 增加独立订阅动态流和未读数量 | 已完成 |
| P4-02 | 支持按最新、未读和热度排序 | 已完成 |
| P4-03 | 支持单条已读、全部已读和作者级推送设置 | 已完成 |
| P4-04 | 建立用户文章合集和合集内排序 | 已完成 |
| P4-05 | 建立管理员专题，支持封面、说明、排序和可见角色 | 已完成 |
| P4-06 | 文章卡片和阅读页展示所属合集与专题 | 已完成 |
| P4-07 | 个人主页支持置顶代表文章和代表合集 | 已完成 |
| P4-08 | 用户可以控制签名、注册时间、统计信息等公开字段 | 已完成 |
| P4-09 | 增加个人主页访问量和隐私友好的访问统计 | 已完成 |
| P4-10 | 完成订阅流、合集、主页的权限与移动端测试 | 已完成 |

### 当前验收结果

- P4-01 至 P4-10 的本地开发、增量迁移、桌面与移动端页面、权限控制和中英文文档已经完成。
- 订阅动态支持最新、未读和热度排序、单条与全部已读以及作者级通知偏好；合集支持增删改查和文章排序；管理员专题支持状态、封面、排序与角色可见范围。
- 文章卡片和阅读页仅向有权限的访问者展示合集与专题；个人主页支持代表文章、代表合集和字段级公开设置，访问统计使用 SHA-256 摘要并按日去重，不保存原始 IP 或 User-Agent。
- 生产数据库已应用增量迁移 `20260810100000_add_discovery_profiles`；30 个 API 测试套件共 194 项通过，API 构建与 Lint、Web 构建与 Lint、Prisma 校验均已通过，Web 仅保留 10 条与本阶段无关的既有警告。
- 已使用 Playwright 检查 1440x900 桌面端和 390x844 移动端的专题、订阅动态、通知偏好、合集、专题管理、资料可见性及公开主页，未发现横向溢出。
- 提交 `ad2fd38` 已推送，GitHub Actions `31351553373` 的 API/Web 镜像构建成功。生产于 2026-08-10 运行 `api-bootstrap`，确认 31 个迁移且无待执行迁移，随后仅重建 API/Web；MySQL、Redis、Caddy、TURN 容器未重启。
- 生产 API/Web 重启次数均为 0，启动日志无错误；首页、健康接口、订阅动态、合集、专题、专题管理和公开专题 API 均返回 `200`，第四阶段完成。
- 2026-08-10 后续体验增强已部署：合集增加权限过滤的全站浏览搜索，合集与专题统一为即时多选和拖动排序，专题支持受管理的本地封面上传与物理清理，移动端专题整卡可点击。提交 `f3d9010` 的 GitHub Actions `31358640865` 成功，生产应用第 32 个迁移后仅重建 API/Web，公开页面、健康接口、容器状态和启动日志验证通过。

### 验收标准

1. 订阅作者发布文章后会进入订阅动态流。
2. 未读状态在不同设备间保持一致。
3. 用户可以创建、排序、修改和删除自己的合集。
4. 管理员专题可以按角色控制可见范围。
5. 个人主页公开字段严格遵守用户隐私设置。

### 暂不包含

- 复杂机器学习推荐算法。
- 付费内容和会员订阅。
- 完全自由的页面搭建器。

## 九、第五阶段：社交能力

目标：在现有好友、私聊、文件和实时消息能力上增加可管理的群聊与临时会话。

### 开发任务

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P5-01 | 建立群组、群成员、群角色和群邀请数据模型 | 已完成 |
| P5-02 | 创建群聊、邀请成员、申请加入和退出群聊 | 已完成 |
| P5-03 | 支持群主、管理员、普通成员的分级权限 | 已完成 |
| P5-04 | 支持群公告、群名称、群头像和群备注 | 已完成 |
| P5-05 | 复用聊天附件能力发送群图片和群文件 | 已完成 |
| P5-06 | 支持禁言、移出、转让群主、解散和成员拉黑 | 已完成 |
| P5-07 | 支持群消息举报和管理端定位处理 | 已完成 |
| P5-08 | 增加临时会话、到期时间和自动清理策略 | 已完成 |
| P5-09 | 完成未读、免打扰、推送和多设备同步 | 已完成 |
| P5-10 | 完成桌面与移动端群聊界面、性能和权限测试 | 已完成 |

### 当前验收结果

- P5-01 至 P5-10 的增量数据模型、API、桌面与移动端群聊界面、群举报管理和生产配置已经完成；已有私聊数据继续使用 `direct` 会话，不需要迁移或重建。
- 群聊默认上限为 100 人，可通过 `CHAT_GROUP_MEMBER_LIMIT` 调整；临时群默认保留 7 天，创建时可选择 1 至 30 天，到期后由每小时执行的受控任务清理会话、附件和群头像。
- 群消息复用现有 Socket.IO 连接和附件存储，通过群会话房间同步，不为每个群建立独立连接；未读游标、免打扰、浏览器推送和多设备已读状态按成员独立保存。
- 群主、管理员和成员权限覆盖群资料、邀请、申请、群内昵称、禁言、移出、拉黑、群主转让、退出、解散、举报处理和活动记录；站点管理员另有群聊举报管理入口。
- 31 个 API 测试套件共 202 项通过，API/Web 构建与 Lint、Prisma 校验、生产 Compose 校验和 `git diff --check` 均通过；Web 仅保留 10 条与本阶段无关的既有警告。
- 已使用外部 Playwright 检查 1440px 桌面端和 390px 移动端的群管理、群举报及“我的创作”页面；长标题、状态标签、元信息和操作区均无横向溢出。
- 生产发布应用增量迁移 `20260811110000_add_group_social_phase`，只更新 API/Web 与迁移容器，不重启 MySQL、Redis、Caddy 或 TURN；健康接口、公开页面、鉴权边界和容器日志验证通过。

### 验收标准

1. 群成员只能执行其角色允许的操作。
2. 群消息、图片和文件能够正常同步和下载。
3. 禁言、移出、退出、解散和举报流程都有审计记录。
4. 临时会话到期后按策略关闭或清理。
5. 群聊不会明显增加空闲状态下的服务器内存占用。

### 暂不包含

- 群语音通话。
- 群视频通话。
- 大型公开聊天室。
- 超过群人数上限的超大群。

## 十、第六阶段：运营后台

目标：为超级管理员和管理员提供可执行的运营数据，并建立面向不同用户范围的公告能力。

### 开发任务

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P6-01 | 建立每日运营聚合任务和聚合数据表 | 已完成 |
| P6-02 | 展示新增用户、活跃用户、文章、评论和消息趋势 | 已完成 |
| P6-03 | 展示热门作者、热门文章、热门搜索和订阅增长 | 已完成 |
| P6-04 | 展示举报、封禁、登录风险和异常任务统计 | 已完成 |
| P6-05 | 增加全站公告、登录用户公告和角色公告 | 已完成 |
| P6-06 | 支持公告草稿、定时发布、自动下线和置顶 | 已完成 |
| P6-07 | 支持公告已读确认、阅读量和未读数量 | 已完成 |
| P6-08 | 公告通过站内消息和可选推送触达用户 | 已完成 |
| P6-09 | 超级管理员与管理员按既有权限共享运营管理 | 已完成 |
| P6-10 | 完成统计口径说明、权限测试和中英文文档 | 已完成 |

### 当前开发结果

- 已完成增量迁移 `20260812170000_add_operations_console`、每日聚合表、排行榜快照、任务记录、小时自动刷新和 7/30/90 天补算。
- 已完成 13 项运营指标、三组趋势图、热门作者/文章/搜索/订阅增长榜单及完整统计口径。
- 已完成公开、登录用户和指定角色公告，支持草稿、定时发布、立即发布、自动下线、归档、置顶、阅读量、确认已读和未读数量。
- 公告通过现有站内系统消息投放，并可独立关闭浏览器推送；投放和待处理事项每日提醒均使用数据库唯一键保证幂等。
- 管理员和超级管理员共享运营数据与公告管理，服务器、安全和备份等既有超级管理员权限不变。
- API 全量 34 个测试套件共 216 项通过；API/Web Lint 与生产构建、Prisma 校验、Compose 配置和差异检查通过。独立说明见 `docs/operations-console.zh-CN.md`。
- 提交 `3ddfd18` 已推送，GitHub Actions `31657738785` 成功。生产已应用第 35 个迁移 `20260812170000_add_operations_console`，运营聚合表已有数据且无失败运营任务；API 健康接口、公告页均返回 `200`，API、Web、MySQL、Redis、Caddy、TURN 均正常运行。
- 外部 Playwright 已通过桌面端与 `390px` 移动端公告页检查，无横向溢出和控制台错误。生产当前没有公告数据，公告投放、确认已读与快捷处理不以制造用户可见测试消息的方式验收，后续有测试公告时补做人工回归；管理员后台可视化登录态也在当前管理员密码可用时补做复核。

### 验收标准

1. 运营数据采用聚合查询，不在页面请求时扫描全量业务表。
2. 每个统计指标都有明确的业务口径和时间范围。
3. 公告能够按公开、登录用户和角色范围投放。
4. 定时发布、自动下线和已读状态准确执行。
5. 管理员无法访问仅超级管理员可见的服务器与安全配置。

### 暂不包含

- 广告投放系统。
- 付费活动和订单系统。
- 独立数据仓库。

## 十一、每个阶段的统一完成标准

每个阶段只有满足以下条件后才能标记为“已完成”：

1. Prisma Schema 与迁移通过生成和校验。
2. API 测试、Lint 和生产构建通过。
3. Web Lint 和生产构建通过。
4. Docker Compose 生产配置校验通过。
5. 桌面端和移动端完成 Playwright 关键流程检查。
6. 权限、登录过期和错误提示行为完成验证。
7. 中英文文档同步更新。
8. 代码已提交并推送，GitHub Actions 镜像构建成功。
9. 生产迁移和部署成功，MySQL、Redis、Caddy、TURN 不被无关重启。
10. 生产健康接口、日志、内存和核心流程验证通过。
11. 本文档对应任务状态和完成记录已经更新。

## 十二、阶段完成记录

| 阶段 | 完成日期 | 提交 | 部署结果 | 备注 |
| --- | --- | --- | --- | --- |
| 第一阶段 | 2026-08-10 | `90e43d3`, `290aa5c`, `fe6c619`, `0569d1a` | P1-01 至 P1-11 已部署（2026-08-05） | 远端恢复功能已完成；用户确认在具备 OSS/R2 服务前暂免真实远端演练，不再阻塞后续阶段 |
| 第二阶段 | 2026-08-07 | `a22a04f`, `8e97c4c`, `c139889` | P2-01 至 P2-11 已部署（2026-08-07） | 非信任设备邮箱验证、取消信任和活动会话逐条退出均已完成真实设备验收 |
| 第三阶段 | 2026-08-10 | `47c7ae3` | 已部署（2026-08-07），Actions `31165692942` 成功 | P3-01 至 P3-11 已完成自动化验证与生产人工验收 |
| 第四阶段 | 2026-08-10 | `ad2fd38`, `f3d9010` | 已部署（2026-08-10），Actions `31351553373`, `31358640865` 成功 | P4-01 至 P4-10 及合集/专题交互增强已完成自动化、桌面/移动端与生产健康验收 |
| 第五阶段 | 2026-08-12 | `1e7881a`, `ca7294c`, `6eb49bc`, `0c6ac56`, `ebdf73a` | 已部署 | P5-01 至 P5-10 及群邀请、举报、媒体和移动端交互增强已完成 |
| 第六阶段 | 2026-08-13 | `3ddfd18` | 已部署，Actions `31657738785` 成功，生产迁移已应用 | P6-01 至 P6-10 已完成自动化、迁移、健康接口与桌面/移动端基础验收；公告投放和管理员后台人工回归在具备测试公告及当前管理员密码时补做 |
| 第七阶段 | 2026-08-14 | `c8b0e3e`, `f813700` | 已部署（2026-08-14），无待执行迁移 | 成对快照、归档校验、恢复预检与恢复后附件扫描已完成；相关 API 单测 14 项、API/Web 构建及生产健康接口均通过 |
| 第八阶段 | 2026-08-14 | `d809d19` | 已部署（2026-08-14），门户偏好迁移已应用 | 首页、工作台、工具中心、个人快捷入口、管理员推荐和移动端恢复弹窗居中已完成；门户定向 API 测试、构建与桌面/移动端无横向溢出检查通过 |
| 第十阶段 P10-01 | 2026-08-21 | `fc183f4` | 已部署（2026-08-21），Actions `32441111237` 成功，无待执行迁移 | 文章、评论和群消息举报已进入统一队列；完整 API 41 个测试套件、270 项通过，桌面/移动端验收、生产健康、新路由与日志检查通过，旧 API/Web 镜像已清理 |
| 第十阶段 | 2026-08-24 | `d4b0d3a`, `ab3edcb` | 已部署，Actions `32681800667`、`32683906735` 成功，P10 迁移已应用 | P10-02 至 P10-04 完成规则自动拦截、批量治理、时限提醒、审计、统计和权限边界；生产健康接口与 `/admin/reports` 均返回 `200`，失败迁移遗留的空表已安全恢复，旧 bootstrap 容器和悬挂镜像已清理 |

## 十三、第七阶段及以后开发计划

### 第七阶段：备份恢复闭环

目标：让本地数据库备份不再脱离上传媒体，恢复前后都有明确、可见、可核查的结果。

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P7-01 | 为每份新建 SQL 备份生成同名媒体快照，并在恢复时追加还原六类上传目录 | 已完成（`c8b0e3e`，2026-08-14） |
| P7-02 | 备份完成后校验 SQL、gzip 和媒体归档可读性，并记录最近校验结果 | 已完成（`f813700`，校验记录与备份文件同目录保存） |
| P7-03 | 在备份列表展示数据库大小、媒体大小、归档状态和校验状态 | 已完成 |
| P7-04 | 恢复前执行附件引用与归档可用性预检，明确提示旧纯 SQL 备份的限制 | 已完成（恢复接口会再次执行预检） |
| P7-05 | 将恢复后缺失附件直接关联到存储管理的修复记录，并完成桌面/移动端回归 | 已完成（恢复后自动启动附件扫描并提供修复入口） |

说明：恢复旧纯 SQL 备份时必须保留当前媒体目录，不能承诺恢复此前已经物理删除的历史附件。

### 第八阶段：首页、工作台和工具中心

目标：以用户每天实际使用的入口组织已有文章、消息、群聊、公告、导航和管理能力，不等待 P9/P10 完成。

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P8-01 | 梳理首页、工作台、工具中心的信息架构与按登录状态、角色展示规则 | 已完成 |
| P8-02 | 重构首页：公告、站点动态、热门内容、专题/合集和常用入口 | 已完成 |
| P8-03 | 重构工作台：我的创作、消息待办、群聊待处理、管理待办和角色相关概览 | 已完成 |
| P8-04 | 重构工具中心：搜索、收藏、排序和按权限筛选的常用工具/导航入口 | 已完成 |
| P8-05 | 支持每个用户保存自己的首页快捷入口与工具排序，管理员维护公共推荐内容 | 已完成 |
| P8-06 | 完成桌面、Android、iPhone 布局和无横向溢出的关键流程验证 | 已完成 |

### 当前实现结果

- 首页改为紧凑的信息聚合页：公告、热门文章、专题/合集和快捷入口均复用现有可见性规则；未设置个人快捷入口时，自动回退到管理员推荐或已有入口。
- 工作台聚合个人文章状态、未读消息、好友申请、群聊审批、评论举报和群聊举报；管理待办仅对管理员和超级管理员显示。
- 工具中心统一检索导航、工具和超级管理员服务器入口。登录用户可收藏最多 30 个条目并拖动排序，服务端会在读写时再次过滤已删除、停用或无权访问的条目。
- 门户条目新增首页推荐标记和排序，管理员可在内容管理的条目编辑中维护；服务器入口不可作为公开推荐。
- 移动端采用单列布局；备份恢复弹窗以视口宽度为边界居中，保留原有高度约束且不产生横向溢出。
- Prisma schema validation、API build、Web build、API/Web lint 和门户定向 API 测试通过。完整 API 套件中 34/35 通过，唯一遗留失败为既有 `chat-attachments.e2e-spec.ts` 与下载方法签名不一致，非 P8 改动引入。

### 第九阶段：隐私与发现扩展

目标：为真实用户增长提供可控的社交边界，并让公开内容更容易被发现。

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P9-01 | 增加个人资料、好友申请、私信和群聊邀请的可见性/接收权限设置 | 已完成 |
| P9-02 | 建立陌生人消息请求箱和统一的黑名单联动行为 | 已完成 |
| P9-03 | 扩展全站搜索至权限内可见的专题、合集、群聊和站点公告 | 已完成 |
| P9-04 | 在发现页增加可订阅的专题、合集和活跃群推荐，并保留权限过滤 | 已完成 |

### 当前实现结果

- 个人中心新增主页、好友申请、陌生私信和群聊邀请权限；设置自动保存，并在个人主页、资料气泡、搜索、好友申请、私信和群邀请入口统一执行。
- 陌生私信默认进入请求箱；接收方可接受或拒绝，接受后生成独立会话并允许从系统消息直达。任一方向拉黑都会取消待处理请求并阻断后续会话和邀请。
- 全站搜索扩展到权限内可见的文章、用户、导航、工具、专题、合集、群聊和站点公告，服务端统一执行登录状态、角色和成员关系过滤。
- 发现页支持专题和合集订阅，并推荐可见专题、合集与尚未加入或封禁的活跃群；专题和合集的新文章会进入订阅流。
- PWA 图标改为以站点设置中的 `pwaIconPath` 为唯一来源，统一页签图标、iOS Apple Touch 图标、动态 Manifest、快捷入口、推送通知和 Service Worker 回退图标，并使用设置更新时间刷新缓存。
- Prisma format/generate/validate、API/Web lint、API/Web build、42 项 P9 定向测试及完整 API 40 个测试套件（259 项）均通过；48 个迁移可在全新数据库顺序执行并完成种子初始化。
- Playwright 已在 390x844 和 1440x900 视口验收个人中心、全站搜索、发现和聊天主窗，未发现页面级横向溢出；隐私设置刷新后可正确回读。
- 提交 `7f591ec` 已推送，GitHub Actions `32221090519` 成功构建并发布 API、Web 镜像。生产已执行两项增量迁移并仅重建 API、Web；外部主页、`/api/health` 和 Manifest 均返回 `200`，API/Web 重启次数为 0，MySQL、Redis、Caddy、TURN 未重启。两份旧 API/Web 无标签镜像已清理，数据卷保持不变。

### 第十阶段：内容治理自动化

目标：在公开用户和群聊增加后，降低人工巡查压力并保留完整处理依据。

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P10-01 | 汇总文章评论、群消息和用户相关举报为统一待处理队列 | 已完成 |
| P10-02 | 增加可配置的敏感词、链接频率、重复内容和高频发言规则 | 已完成 |
| P10-03 | 支持批量处理、处理模板、处理时限和自动通知 | 已完成 |
| P10-04 | 补充审核操作审计、统计和管理员权限边界测试 | 已完成 |

### 当前实现结果

- 新增统一举报查询与汇总接口，将文章、评论和群消息举报按创建时间合并为同一条待处理队列，支持来源、状态和分页筛选。
- 新增管理员举报中心、顶部待办举报气泡和工作台统一入口；账户菜单保留独立举报中心入口，用户反馈仍使用原有独立流程。
- 处理和驳回继续调用文章、评论与群消息的原有处理接口，保留通知、积分、消息删除和重复处理保护等既有业务逻辑。
- 已新增敏感词、链接频率、重复内容和高频发言四类规则；规则可限定文章、评论和群消息来源，支持仅记录或记录并拦截。草稿、自动保存和私信不进入规则检查，成功发布后才记录内容指纹与频率数据。
- 举报中心支持同来源待处理举报的批量处理、可复用处理反馈模板、处理时限和临近/超时的幂等站内提醒；批量处理默认保留原内容，屏蔽、删除等破坏性操作仍需逐条确认。
- 已增加规则命中、待处理、超时、处理时长和按来源统计。站点管理员可查看统计、命中、规则和模板并处理举报；只有超级管理员可以新增、编辑或删除规则、模板和时限设置。
- `/moderation` 的新增、修改、删除和批量处理操作进入业务审计；新增规则引擎、时限提醒、批量分流、审计分类和超级管理员边界测试。
- Prisma generate/validate、API/Web lint 与生产构建通过；完整 API 43 个测试套件、278 项测试通过。
- Playwright 已在 1280px 桌面和 390x844 移动视口检查统一列表、顶部待办、筛选和处理弹窗，未发现页面级横向溢出。
- 提交 `fc183f4` 已推送，GitHub Actions `32441111237` 成功发布 API/Web 镜像。生产 bootstrap 确认 49 个迁移全部已应用且无待执行迁移；仅重建 API/Web，外部主页、`/admin/reports` 和 `/api/health` 均返回 `200`，六个核心容器重启次数均为 0，两份旧 API/Web 镜像和停止的 bootstrap 容器已清理。
- 提交 `d4b0d3a`、`ab3edcb` 已推送，GitHub Actions `32681800667`、`32683906735` 成功发布 API/Web 镜像。生产环境的 P10 新迁移首次因 MySQL 索引名长度限制中断；核验六张新增表均为空后，仅移除这些孤立表并以修正索引名完整重跑迁移。API、Web 已重新创建，`/api/health` 与 `/admin/reports` 均返回 `200`；停止的 bootstrap 容器和悬挂镜像已清理，释放约 2.86 GB，MySQL、Redis、Caddy、TURN 及全部数据卷保持不变。

### 第十一阶段：产品稳定性收尾

目标：完成稳定性收尾、积分资源闭环、性能专项和运营推荐增强，并为核心流程建立可重复的桌面、移动端和线上验证。

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P11-01 | 补充聊天菜单、我的举报和个人中心关键布局的桌面/移动端回归验证 | 已完成 |
| P11-02 | 积分、经验与资源闭环：待入账、72 小时结算、重复兑换保护和资源文章体验 | 已完成 |
| P11-03 | 性能专项：首屏、分页、推荐缓存、定时任务和图片资源的回归检查 | 已完成 |
| P11-04 | 运营与推荐增强：推荐内容、匿名话题统计和通知转化统计 | 已完成 |

### P11-01 验收记录

- 新增 `scripts/p11-01-playwright.py`，通过 mock API 数据验证聊天列表三个点菜单使用 Portal 后保持在聊天窗层级之上；桌面视口验证菜单可命中，390px 移动视口验证菜单不溢出且页面无横向滚动。
- 验证“我的举报”标题/标签/时间第一行、详情第二行占满主列宽度，超长详情省略后可通过无边框磨砂提示查看完整文本，并覆盖桌面与移动视口。
- 验证个人中心展开登录设备后，“登录设备”区块位于“成长与积分”之前，中文界面与移动布局均通过。
- 本地 Playwright 两个视口通过；Web lint 通过（保留 23 条既有 warning）、TypeScript、生产构建和 `git diff --check` 通过。

### P11-02 验收记录

- 保留按阅读、评论、发布、获赞、订阅和举报采纳累计经验/积分的既有规则；资源区块继续以 `:::resource{points=N}` 和结束标记 `:::` 定义，购买方扣分后永久解锁对应区块。
- 作者资源收入以不可变账本记录为待入账积分，`availableAt` 设为兑换后 72 小时；定时结算以条件更新先领取账本记录，避免多实例或重复定时任务重复入账，同时回写资源兑换记录的结算时间。
- 同一用户、文章和资源区块由数据库唯一索引保护；并发命中唯一约束时返回“该资源已经兑换，无需重复支付”，不会重复扣除买方积分或重复创建作者待入账。
- 新增并通过资源兑换重复保护、72 小时结算、并发结算领取和运营统计单元测试。

### P11-03 验收记录

- 延续已有的文章/专题/合集分页、推荐结果 Redis 五分钟缓存、后台定时聚合、媒体资源长期缓存头和单任务备份/扫描边界，避免首页和运营页面读取全量关系数据。
- 新增 `scripts/p11-browser-smoke.py`，以只读 Chromium 检查中英文首页、发现、专题和合集的首次加载时间、HTTP 500、浏览器异常、控制台错误、失效图片和移动端横向溢出；默认阈值为 12 秒，可通过 `LINGXI_MAX_PAGE_LOAD_MS` 调整。
- 本地生产构建在 1440x900 与 390x844 两个视口共 16 个公开页面均通过，首次文档加载为 40ms 至 371ms。

### P11-04 验收记录

- 保留文章个性化推荐及专题、合集、活跃群聊推荐，推荐排序使用阅读/点赞/收藏/稍后读、订阅作者、互动热度、发布时间和置顶状态，并通过 Redis 缓存及可见性复核保证性能和权限边界。
- 运营数据继续按中国时区自然日聚合匿名话题、发言、点评获赞和话题喜欢；新增通知创建、已读和打开三项指标，以及已读率、打开率，创建、已读和打开按各自的时间字段统计，避免把未读通知误计为转化。
- 运营数据页面已展示通知创建、已读、打开及通知转化摘要；中英文固定文案同步配置。

### 第十二阶段：增长与内容转化

目标：以可撤回的兴趣选择、可控的订阅触达和可检索的积分资源，提升新用户完成首次内容发现和资源兑换的路径。

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P12-01 | 新用户首登兴趣引导：选择最多 3 个可见专题和 6 位推荐创作者并批量订阅，可显式跳过 | 已完成 |
| P12-02 | 订阅日报偏好：独立开关、可见性过滤和按中国日期幂等投递 | 已完成 |
| P12-03 | 全站搜索无结果时提供热门搜索、可见专题和积分资源继续浏览 | 已完成 |
| P12-04 | 积分资源中心：资源目录、最低积分、兑换热度和个人兑换/待入账摘要 | 已完成 |

### P12 验收记录

- 首登引导仅向已登录且尚未完成选择的用户展示；可见专题最多选择 3 个，并可选择最多 6 位按公开文章数、阅读、评论、点赞、收藏和近期活跃度综合推荐的创作者。两类选择均支持批量订阅和显式跳过，蒙版点击不会关闭引导。
- 订阅日报开关位于消息设置；按中国日期去重生成站内提醒，并在生成前重新校验文章与专题的当前可见性和角色访问权限。
- 搜索无结果时提供热门搜索、可见专题及积分资源入口；资源中心支持检索、排序、最低积分和兑换热度，并展示个人兑换、销售和待入账汇总。
- 资源内容块自动同步文章资源标记和最低积分。Prisma 校验、API 287 项测试、API/Web lint 与生产构建、P12 Playwright 桌面/移动视口均已通过。

开发顺序固定为 P7 剩余任务 -> P8 首页/工作台/工具中心 -> P9 -> P10 -> P11；每阶段完成后均按本路线图的统一完成标准独立验收、部署和记录。

## 十四、后续任务恢复规则

以后继续这份计划时，执行顺序固定为：

1. 读取本路线图和对应英文文档。
2. 检查仓库当前代码、生产状态和未提交修改。
3. 找到当前阶段中编号最靠前的“未开始”或“进行中”任务。
4. 核对前置配置是否齐全。
5. 完成实现、测试、推送和部署。
6. 更新任务状态、阶段总览和完成记录。

不得仅根据旧对话记忆判断完成情况，必须以仓库代码、数据库迁移、生产验证和本文档记录为准。
