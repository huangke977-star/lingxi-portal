export default function DashboardPage() {
  return (
    <section>
      <span className="eyebrow">Workspace</span>
      <h1>个人工作台</h1>
      <p>登录后将展示按角色过滤后的导航、页面和工具。</p>
      <div className="status-row">
        <span className="status">等待认证模块</span>
      </div>
    </section>
  );
}
